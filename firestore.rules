rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for the setareh-shahr application collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
